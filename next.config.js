/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'i.ytimg.com',  // YouTube thumbnails
      'img.youtube.com'
    ],
  },
}

module.exports = nextConfig
